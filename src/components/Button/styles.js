import styled from 'styled-components';

export const Container = styled.button`
  width: 220px;
  height: 40px;
  border-radius: 5px;
  margin: 30px auto;
  border: none;

  &:hover {
    filter: brightness(0.9);
  }
`;